seed: 42
seeds:
  - 42
  - 43
  - 44
device: auto
comparison:
  primary: exp_bce
  references: [baseline]
epochs: 50
early_stopping:
  enabled: true
  monitor: mAP
  mode: max
  patience: 10
  min_delta: 0.001
  min_epochs: 10
batch_size: 64
learning_rate: 0.001
num_workers: 4
image_size: 224
compile: true
max_train_samples:
max_val_samples:
output_dir: outputs
best_model_name: best_model.pth
metrics_name: metrics.csv

