seed: 42
seeds:
  - 42
  - 43
  - 44
device: auto
comparison:
  primary: exp_freeze_resnet101
  references: [exp_gradual-unfreeze_resnet18, exp_unfreeze_resnet101,exp_partial_freeze_resnet101,baseline]
epochs: 50
early_stopping:
  enabled: true
  monitor: mAP
  mode: max
  patience: 10
  min_delta: 0.001
  min_epochs: 20
batch_size: 64
learning_rate: 1e-5
num_workers: 2
image_size: 224
compile: true
max_train_samples:
max_val_samples:
output_dir: outputs
best_model_name: best_model.pth
metrics_name: metrics.csv
